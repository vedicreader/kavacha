# probe


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

What this machine can build and run: the interpreter, the window, and
what is already running.

Every answer here is about the machine the code runs on, so every answer
differs per platform. The platform and the interpreter are parameters
rather than lookups:
[`backend`](https://vedicreader.github.io/kavacha/probe.html#backend)
and
[`shell_ready`](https://vedicreader.github.io/kavacha/probe.html#shell_ready)
take a platform name, and
[`py_version`](https://vedicreader.github.io/kavacha/probe.html#py_version),
[`is_framework`](https://vedicreader.github.io/kavacha/probe.html#is_framework)
and
[`framework_python`](https://vedicreader.github.io/kavacha/probe.html#framework_python)
take interpreter paths. The decisions can be exercised from anywhere.

This module imports nothing but fastcore. It never imports pywebview,
py2app or py2exe, so asking what a machine could build costs none of
them.

A platform absent from `BACKENDS` has no native window, and
[`shell_ready`](https://vedicreader.github.io/kavacha/probe.html#shell_ready)
says so before anything tries to open one.

`FRAMEWORK_CANDIDATES` is read on macOS only, and nothing checks the
paths exist until
[`framework_python`](https://vedicreader.github.io/kavacha/probe.html#framework_python)
walks them. The order is preference, not recency: 3.13 before 3.14
because the version in the list is the version the app ships.

------------------------------------------------------------------------

<a
href="https://github.com/vedicreader/kavacha/blob/main/kavacha/probe.py#L38"
target="_blank" style="float:right; font-size:smaller">source</a>

### shell_ready

``` python
def shell_ready(
    platform:NoneType=None
):
```

*`(ok, why)`: whether a native window can be opened here, and what is
missing if not.*

------------------------------------------------------------------------

<a
href="https://github.com/vedicreader/kavacha/blob/main/kavacha/probe.py#L34"
target="_blank" style="float:right; font-size:smaller">source</a>

### backend

``` python
def backend(
    platform:NoneType=None
):
```

*pywebview’s GUI name for `platform`, or None where there is no
supported webview.*

[`shell_ready`](https://vedicreader.github.io/kavacha/probe.html#shell_ready)
answers two separate questions in one pair. `ok` is False for a platform
with no backend and for a missing pywebview, and `why` names which of
the two it was. When `ok` is True, `why` is the pywebview GUI name,
which is what the caller passes on as `gui`.

Importing `webview` is how it finds out. That import happens here and
nowhere else in the module.

``` python
backend('darwin'), backend('linux'), backend('plan9')
```

    ('cocoa', 'gtk', None)

``` python
test_eq(shell_ready('plan9'), (False, 'no native webview backend for plan9'))
ok, why = shell_ready('darwin')
test_eq(ok, why == 'cocoa')   # the gui name when a window can be opened, what is missing when not
```

------------------------------------------------------------------------

<a
href="https://github.com/vedicreader/kavacha/blob/main/kavacha/probe.py#L69"
target="_blank" style="float:right; font-size:smaller">source</a>

### framework_python

``` python
def framework_python(
    candidates:tuple=('/Library/Frameworks/Python.framework/Versions/3.13/bin/python3.13', '/Library/Frameworks/Python.framework/Versions/3.14/bin/python3.14', '/Library/Frameworks/Python.framework/Versions/3.12/bin/python3.12', '/opt/homebrew/opt/python@3.13/bin/python3.13', '/opt/homebrew/opt/python@3.14/bin/python3.14', '/opt/homebrew/opt/python@3.12/bin/python3.12', '/usr/local/opt/python@3.13/bin/python3.13', '/usr/local/opt/python@3.14/bin/python3.14', '/usr/local/opt/python@3.12/bin/python3.12', '/usr/bin/python3')
):
```

*The first framework interpreter on this machine that py2app can build
against.*

------------------------------------------------------------------------

<a
href="https://github.com/vedicreader/kavacha/blob/main/kavacha/probe.py#L55"
target="_blank" style="float:right; font-size:smaller">source</a>

### is_framework

``` python
def is_framework(
    python:NoneType=None
):
```

*Whether `python` (default: this interpreter) is a macOS framework build
py2app can use.*

------------------------------------------------------------------------

<a
href="https://github.com/vedicreader/kavacha/blob/main/kavacha/probe.py#L47"
target="_blank" style="float:right; font-size:smaller">source</a>

### py_version

``` python
def py_version(
    python
):
```

*The `(major, minor)` of `python`, or None when it will not say.*

[`py_version`](https://vedicreader.github.io/kavacha/probe.html#py_version)
and
[`is_framework`](https://vedicreader.github.io/kavacha/probe.html#is_framework)
ask another interpreter about itself by running it. An interpreter that
is missing, is not a Python, or refuses to start is answered rather than
raised: `None` from
[`py_version`](https://vedicreader.github.io/kavacha/probe.html#py_version),
False from
[`is_framework`](https://vedicreader.github.io/kavacha/probe.html#is_framework).

[`is_framework`](https://vedicreader.github.io/kavacha/probe.html#is_framework)
given a path applies a version floor, and a framework build below 3.12
answers False.
[`is_framework`](https://vedicreader.github.io/kavacha/probe.html#is_framework)
with no argument reads this process’s own `sysconfig` and applies no
floor, so the two forms can disagree about this very interpreter.

[`framework_python`](https://vedicreader.github.io/kavacha/probe.html#framework_python)
returns the first candidate that exists and passes
[`is_framework`](https://vedicreader.github.io/kavacha/probe.html#is_framework),
as a `Path`. The order of the candidate list is the whole of the
preference. None means the machine has no framework build, which is
every Linux machine and most macOS ones until someone installs one.

Three interpreters that answer the probe differently: a framework build
py2app can embed, one on a Python too old, and a python-build-standalone
whose `PYTHONFRAMEWORK` is empty. The last is what uv and pyenv install.

``` python
is_framework(fw), is_framework(old), is_framework(pbs)
```

    (True, False, False)

------------------------------------------------------------------------

<a
href="https://github.com/vedicreader/kavacha/blob/main/kavacha/probe.py#L74"
target="_blank" style="float:right; font-size:smaller">source</a>

### wait_for_http

``` python
def wait_for_http(
    url, timeout:int=60, interval:float=0.1
):
```

*Block until `url` answers, or `timeout` passes. True if the server came
up.*

[`wait_for_http`](https://vedicreader.github.io/kavacha/probe.html#wait_for_http)
polls until the server answers. Every exception from the request is a
not-yet, so a connection refused while the server is still binding its
port is not a failure. False means `timeout` passed. It never raises.

An error status is not an answer. `urlopen` raises for a 404, and that
is caught with the rest, so a URL that is served but wrong waits out the
full timeout.

``` python
test_eq(wait_for_http(url), True)
test_eq(wait_for_http(url + 'missing', timeout=.3, interval=.05), False)   # 404 is not an answer
```

------------------------------------------------------------------------

<a
href="https://github.com/vedicreader/kavacha/blob/main/kavacha/probe.py#L86"
target="_blank" style="float:right; font-size:smaller">source</a>

### running_from

``` python
def running_from(
    bundle, ps_output:NoneType=None
):
```

*The pids of processes running out of `bundle`, so a build does not
overwrite a live app.*

py2app writes the bundle in place, and `python313.zip` is the running
app’s standard library. Replacing it under a live process leaves every
import it has not made yet reading a file that is no longer the one it
opened: `ZipImportError: bad local file header`, raised by whatever
happens to import next and naming nothing that would lead anyone back to
a rebuild.

[`running_from`](https://vedicreader.github.io/kavacha/probe.html#running_from)
takes `ps` output as an argument, so the decision can be exercised
without a live app. Left out, it runs `ps -Ao pid,command` itself and
returns `[]` when that fails.

The first line of `ps_output` is dropped as the header.

A row matches when its executable is inside the bundle directory. Two
things narrow that. The comparison uses the first field of the command
rather than the whole line, because this check names the bundle in its
own arguments and so does every grep, editor and shell that has the path
open. It compares against the bundle path plus a separator, so a sibling
whose name starts the same way does not match. This process is never in
the result.

``` python
ps = """  PID COMMAND
  101 /Apps/Demo.app/Contents/MacOS/Demo --serve
  102 /usr/bin/grep -r /Apps/Demo.app
  103 /Apps/Demo.app.old/Contents/MacOS/Demo
  104 /bin/zsh
"""
running_from('/Apps/Demo.app', ps)
```

    [101]
